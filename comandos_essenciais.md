# Comandos Essenciais do Linux

## 📌 Comandos de navegação
- `ls` → lista arquivos e diretórios  
- `cd` → muda de diretório  
- `pwd` → mostra o caminho atual  

## 📌 Manipulação de arquivos e pastas
- `mkdir` → cria diretório  
- `rm` → remove arquivo  
- `rm -r` → remove diretórios  
- `mv` → move ou renomeia  
- `cp` → copia arquivos  

## 📌 Permissões
- `chmod` → altera permissões  
- `chown` → altera dono do arquivo  

## 📌 Sistema
- `top` → processos em execução  
- `df -h` → uso de disco  
- `du -sh` → tamanho de pastas  
- `ps aux` → lista processos  

## 📌 Redes
- `ifconfig` ou `ip a` → interfaces  
- `ping` → teste de rede  
- `netstat -tulnp` → portas abertas  
- `curl` → requisições HTTP  
